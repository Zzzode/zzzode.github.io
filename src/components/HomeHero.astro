---
import { type Lang, t } from '../i18n/ui';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const prefix = lang === 'en' ? '/en' : '';
---

<section class="bg-white">
  <div class="container-page flex flex-col items-center pb-14 pt-16 text-center sm:pb-16 sm:pt-20">
    <p class="eyebrow">{t(lang, 'home.eyebrow')}</p>
    <h1
      class="mt-6 max-w-[900px] text-[clamp(40px,7vw,72px)] font-semibold leading-[1.04] tracking-[-0.035em]"
    >
      <span class="block text-ink">{t(lang, 'home.titleA')}</span>
      <span class="block text-muted">{t(lang, 'home.titleB')}</span>
    </h1>
    <p class="mt-7 max-w-[560px] text-[17px] leading-relaxed text-muted sm:text-[19px]">
      {t(lang, 'home.lead')}
    </p>
    <div class="mt-9 flex flex-wrap items-center justify-center gap-x-8 gap-y-3">
      <a
        href={`${prefix}/posts/`}
        class="inline-flex items-center gap-1 text-[15px] font-medium text-blue transition-colors hover:text-blue-hover"
      >
        {t(lang, 'home.viewWriting')}
        <span aria-hidden="true" class="text-[1.05em] leading-none">›</span>
      </a>
      <a
        href={`${prefix}/cv/`}
        class="inline-flex items-center gap-1 text-[15px] font-medium text-ink-2 transition-colors hover:text-blue"
      >
        {t(lang, 'home.viewCv')}
        <span aria-hidden="true" class="text-[1.05em] leading-none">›</span>
      </a>
    </div>
  </div>
</section>
